// import Graph, { Edge, Node, PinInfoMap, PinInfo } from "../Circuit.Graph";

function deriveCurrent(
  // edge: Edge,
  // node: { 'POSITIVE': Node }, // , 'NEGATIVE': Node },
  // graph: Graph,
) {
  /* TODO: Try to implement DCSource version of deriveCurrent */
}

export const DCSource = {
  deriveCurrent
};
